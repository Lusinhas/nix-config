# Nix Config

Just a NixOS config I made for personal usage.

# Usage

- Bootable ISO generation: `nix build .#iso`
- Installation: boot into the generated ISO and run `sudo install-nixos`, then follow what it says to continue.
